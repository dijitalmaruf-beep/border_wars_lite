rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /games/{gameId} {
      // MVP online rooms use short room codes and no auth yet.
      // Tighten this before public production release.
      allow read, write: if true;
    }
  }
}
